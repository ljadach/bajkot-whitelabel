# 2026-03-03

## Co zrobione

- Zmergowany PR #112 (`feature/prompt-playing`) na staging — tightening HandbookSystem prompt: krótsze rozdziały (do 300/400 słów zamiast 700-1200), dodane reguły jakości pisania (Clarity Over Cleverness, Active Voice, Scannability 80/20, etc.)
- Rozwiązane konflikty w `convex/lib/prompts/courseFallbacks.ts` — staging miał refactor promptów v2, PR chciał skrócić rozdziały i dodać editorial rules. Wzięte krótsze word counts z PR-a, zachowane strukturalne ulepszenia ze staginga (skill gap calibration, source hierarchy, competitive framing)
- Prettier cleanup: `pipelineConfig.ts`, `DebugTabConfig.tsx`, `i18n.ts`, `courseAi.ts` — same formatowanie, zero zmian logicznych

## Notatki techniczne

- `courseFallbacks.ts` HandbookSystem vs HandbookSystemVideoEnhanced — video-enhanced wariant wciąż ma stare word counts (700-1200). Jeśli intencja jest spójna, trzeba będzie zsynchronizować te dwa prompty.
