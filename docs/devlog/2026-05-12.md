# 2026-05-12

## Sync cen z Stripe (PDF 29 zł, druk 49 zł)

Stripe trzyma kanoniczne ceny — produkty w katalogu mają PDF 29 PLN i wydruk 49 PLN. Kod miał odziedziczone 39/59 z poprzedniej iteracji. Dwa źródła stałych (źle, ale tak jest):

- `src/lib/pricing.ts` — frontend display (cards, comparison table, schema.org Offers, JSON-LD, paywall CTA, Cennik headline)
- `convex/lib/email.ts` — duplikat dla maili transakcyjnych (Resend), bo Convex Node runtime nie chciał importu z `src/`

Plus rozsiane copy w trzech miejscach: pl/book.json (formatPdfPrice/formatPrintPrice używane w starym book-order flow), testimonials w `cennik.ts` i meta description w `routes/cennik.tsx`.

Co zaktualizowane:

| Plik                       | Zmiana                                 |
| -------------------------- | -------------------------------------- |
| `src/lib/pricing.ts`       | 39 → 29, 59 → 49                       |
| `convex/lib/email.ts`      | 39 → 29, 59 → 49 (lustrzane stałe)     |
| `src/locales/pl/book.json` | "39 zł" → "29 zł", "59 zł" → "49 zł"   |
| `src/data/cennik.ts`       | 2 testimoniale + FAQ shipping (3 hity) |
| `src/routes/cennik.tsx`    | meta description                       |

Delta druk−PDF zostaje 20 zł (49−29 = 59−39), więc FAQ w `pl/faq.json` o "dodatkowe 20 zł za druk" nadal poprawne.

Świadomie nietknięte:

- `convex/lib/prompts/bookFallbacks.ts:1788` — upsell "Pakiet 3 bajek za 99 zł (oszczędzasz 18 zł)". To produkt nieistniejący w Stripe, więc poza scope sync — flaga na osobny ticket.
- `docs/spec-topic-landing-pages.md`, `docs/amazon-print-guide.md` — historyczne docs/plany.

## Sanity check

`npx tsc -p convex && npx tsc -p . && eslint src/**/*.{ts,tsx} && react-router build` — wszystko zielone. Stripe price ID w env (`STRIPE_BOOK_PRICE_ID`) jest dalej źródłem prawdy dla faktycznej kwoty pobieranej — te stałe są display-only.
