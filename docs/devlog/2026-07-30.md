# 2026-07-30

## Poprawki z testów Trello (board bajkoterapiaorg) + nowy homepage na LP v4

Runda poprawek po testach Łukasza z 29.07 (karty z TODO na boardzie
`bajkoterapiaorg`), branch `pr`, deploy na prod tego samego dnia. Zasada:
wszystko, co było jednoznaczne, poszło od razu; kwestie decyzyjne wróciły na
board jako Questions/On Hold z komentarzem.

- **Shipping copy**: `DELIVERY_DAYS_TEXT` 3–5 → 5–10 dni roboczych (LP mówił co
  innego niż order flow, który od dawna obiecuje „do 10 dni"). Przy okazji
  „wysyłka po Polsce" → „wysyłka w Polsce" w hero, cenniku i FAQ.
- **Hero pricing**: przekreślone 59 zł przy cenie PDF w trust bulletach +
  gwiazdka Omnibus („najniższa cena z ostatnich 30 dni: 49 zł"). Bullety
  przepisane ze stringów na JSX — cena przestała być zlepkiem template
  stringów.
- **Modal przykładowej bajki obcinał okładkę**: `BookPdfFlipbook` miał sztywny
  kontener `aspect-[5/7]` z `overflow-hidden`, a sample PDF jest A4 (1.4145 ≠
  1.4). Fix systemowy: aspect ratio kontenera mierzony z realnej strony PDF
  (`Page.onLoadSuccess` → `originalWidth/originalHeight`), fallback 5/7 na czas
  ładowania. Działa dla dowolnego formatu, także kwadratowych stron composera.
- **Sample PDF z pełną okładką**: strona 1 `maja-bajka.pdf` była złamana
  (grafika ucięta w połowie + biała dziura). Przebudowana skryptem pymupdf:
  full-bleed `maja-cover.webp` (proporcje niemal idealnie A4) + pasek z tytułem
  (Georgia Bold, polskie znaki). Reszta stron nietknięta.
- **Filmik promo**: pierwsza sekunda to było puste tło — przycięte ffmpegiem
  (`-ss 1`, re-encode x264 CRF 20), plik przy okazji schudł z 7.2 MB do 5.6 MB.
- **Drobne copy**: „rozwiązać zagadkę i rozwinąć przygodę" (karta Nauka=klucz w
  zrozumienie-celu-nauki), wycięte „zanim cokolwiek zapłacisz", okładka sample
  book wyśrodkowana i powiększona (240→300 px).
- **Nowy homepage na LP v4**: stary index (calm/magic, i18n, ilustracje
  „floating book") wymieniony na design system podstron `/problem/*`: hero w
  stylu v4 → siatka 39 tematów z filtrami kategorii (katalogowe emoji +
  shortTitle, linki do `/problem/:slug`) → współdzielone sekcje product /
  video / reviews / safety / FAQ / pricing. Homepage nie ma inline wizarda,
  więc wszystkie CTA celują w `#tematy`; telemetria idzie z pseudo-slugiem
  `home`. Sekcje v4 dostały opcjonalne `ctaHref` / zawężone propsy
  (`Pick<Topic,'slug'>`), więc topic pages działają bez zmian.
- **Kafelki science („align the tiles")**: nie odtworzone — grid już
  stretchuje karty do równej wysokości; screenshot testera pochodził pewnie ze
  starej wersji albo proto. Karta wróciła na board do re-testu.
- **Deploy gotcha**: `npm run deploy` w tle wysypuje się na interaktywnym
  prompcie convexa („push code to prod?") i **kończy exit 0 bez odpalenia
  vercela** — cichy fail. Headless zawsze: `convex deploy --cmd 'npm run
build' --yes && vercel --prod`.

Poza kodem: faktury uznane za temat po stronie Stripe (won't do w repo — brak
jakiejkolwiek integracji fakturowej, `invoice_creation` wyłączone), Google Ads
purchase tag czeka na Conversion ID/Label od Łukasza, przeniesienie wyboru
formatu za podgląd bajki wstrzymane jako decyzja produktowa (brainstorm:
`docs/conversion-brainstorm-generate-before-payment`).
